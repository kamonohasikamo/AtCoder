#include <stdio.h>

int main(){
  int n,a,b;
  scanf("%d %d %d",&n,&a,&b);
  int i;
  for(i=0;i<n;i++){

  }

  return 0;
}

int sum;
for (i=0;;i++){
  sum += n % 10;
  n /= 10;
  if(n < 0) break;
}
